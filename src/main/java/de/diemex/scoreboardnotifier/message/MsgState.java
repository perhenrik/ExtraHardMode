package de.diemex.scoreboardnotifier.message;


/**
 * This enum shows if a message is scheduled, being shown or not being shown anymore
 */
public enum MsgState
{
    SCHEDULED,
    NOT_SHOWING,
    SHOWING
}
